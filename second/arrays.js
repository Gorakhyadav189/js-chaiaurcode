const array=[1,2,3,4,5,6,7];

array.push(10);
// array.pop()->is alwayes pop the last vindex value
// array.push()->alwayes insert the value in last index
// array.unshift()insert the value oi 0 index
// array.shift()drop the value of first index
// (array.includes(2)->to check the numer is in my array or not
// console.log(array.includes(2))
// .join()->is used to convert array to String
// const newarray=array.join();
// console.log(array);
// console.log(newarray);
